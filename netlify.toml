# Netlify Configuration for TaskFlow Next.js App

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# Use Netlify's Essential Next.js plugin
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Configure serverless functions
[functions]
  node_bundler = "esbuild"


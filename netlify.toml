# Netlify Configuration for TaskFlow Next.js App

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# Use Netlify's Next.js plugin (handles API routes automatically)
[[plugins]]
  package = "@netlify/plugin-nextjs"

